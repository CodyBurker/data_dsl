{
  "name": "data_dsl",
  "version": "1.0.0",
  "description": "DSL for manipulating CSV data in the browser",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "danfojs-node": "^1.2.0",
    "jsdom": "^26.1.0"
  }
}
